; Test script for ruse.

(= multiply (quote multiply))

(= (multiply (int @x) (int @y))
	 (multiply-int x x))

(= square (quote square))

(= (square (@t @x))
	 (multiply (t x) (t x)))

;(square (int 8))
;(cond
;	((builtin eq? 'dog 'dog) (int 1))
;	(else (int 0)))

(=* (if @c @t @f)
		(cond (c t) (else f)))

(if (builtin eq? 1 1)
	(int 1)
	(int 0))
